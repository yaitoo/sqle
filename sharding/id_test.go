package sharding

import "testing"

func TestID(t *testing.T) {
	tests := []struct {
		name string
	}{
		{
			name: "build_should_work",
		},
	}

	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {

		})
	}
}
